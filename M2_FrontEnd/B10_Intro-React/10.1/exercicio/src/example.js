const Task = (value) => {
  return value
}

const tasksArray = ['Acordar', 'Tomar café', 'Estudar', 'Almoçar', 'Ver vídeos'];

// tasksArray.map((task) => console.log(task));

console.log(Task(tasksArray));